<UserControl x:Class="FlightSimulator.Views.VerticalSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlightSimulator.Views"
             mc:Ignorable="d" Width="43.222" Height="228.667">
    <Grid DockPanel.Dock="Top" Margin="0,0,0,10" >

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Slider Name="Slider" Foreground="Blue" Grid.Column="0"
            Minimum="-1" Maximum="1"
            Orientation="Vertical"
            Grid.ColumnSpan="2" Margin="0,0,10,0"/>
        <TextBox Text="{Binding Value, ElementName=Slider, UpdateSourceTrigger=PropertyChanged}" Height="20" Margin="17,10,0.333,188.667" Grid.ColumnSpan="2" />
    </Grid>
</UserControl>
